import { render } from '@testing-library/react'
import App from '../src/App'

describe('Initial test case for App component', () => {
    it('Renders successfully', () => {
        render(<App />)
    })
})
